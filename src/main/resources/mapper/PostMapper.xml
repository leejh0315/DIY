<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper	namespace="project.DIY.repository.mybatis.PostMapper">

<!-- 로그인 -->
<!-- id는 인터페이스 메소드명과 동일 -->
	<insert id="insertPost" parameterType="Post">
		INSERT INTO diydb.post
		VALUES (
			null, 
			#{postCtcode},
			#{targetName}, 
			#{targetSubName},
			#{targetAuthor},
            #{targetThumbnail},
            #{memberId},
            #{memberNick},
            #{starCount},
            #{title},
            #{content},
			date_format(now(), '%Y%m%d%H%i%s'),
			null,
			"Y"
			);

	</insert>
	
	
	<select id="selectPost" resultType="Post">
		insert INTO diydb.post VALUES (
		#{title}, #{content}
		);

	</select>
	
	<select id="getLastPost" resultType="String">
		SELECT postCode FROM diydb.post ORDER BY createOn DESC LIMIT 1;
	</select>
	
	<select id="selectByPostCode" resultType="Post">
		select * from diydb.post where postCode = #{postCode};
	</select>
	
	<select id="selectByType" resultType="Post">
		select * from diydb.post where postCtCode = #{type}
		order by postcode DESC limit 0,5;
	</select>
	
<<<<<<< HEAD
	
	<select id="selectUserPostbyId" resultType="Post">
		select * from diydb.post where memberId= #{memberId};
=======
	<select id="selectByPostCtCodeHome" resultType="Post">
		select
			 targetName,
			 targetAuthor,
			 targetThumbnail,
			 avg(starCount) as starCount,
			 targetSubName,
			 count(targetName)
			  as cnt 
			 from diydb.post
			where postCtCode = #{postCtCode}
			 group by targetName order by cnt desc;
		
>>>>>>> c8323e938397b24466f62554e3c5b290c7d36b97
	</select>
</mapper>