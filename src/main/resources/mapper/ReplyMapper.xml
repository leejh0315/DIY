<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper	namespace="project.DIY.repository.mybatis.ReplyMapper">

<!-- 로그인 -->
<!-- id는 인터페이스 메소드명과 동일 -->
	<insert id="insertReply" parameterType="Reply">
		INSERT INTO diydb.reply (replyId, postId, replyContent, replyerId, replyCreateDate, replyUpdateDate)
		VALUES (
			null , 
			#{postId}, 
			#{replyContent}, 
			#{replyerId}, 
			#{replyCreateDate},
			null
		);
		

	</insert>
	
	<select id="getReply" resultType="Reply">
		select * from diydb.reply where postId=#{postId} order by replyId asc;
	</select>
	
	<select id="selectNickname">
		select nickName,profileSrc  from diydb.member where id = #{replyerId};
	</select>
	
	<insert id="insertReReply" parameterType="Reply">
		INSERT INTO diydb.rereply
		VALUES (
			null, 
			#{postId}, 
			#{replyContent}, 
			#{replyerId}, 
			#{replyCreateDate},
			null
		);
	</insert>
	
	<select id="selectReReplyById" resultType="ReReply">
		select * from diydb.rereply where replyId = #{param1};
	</select>
	
	<delete id="deleteReplybypostCode" parameterType="Integer">
		delete from diydb.reply where postId=#{postId};
	</delete>
	
</mapper>