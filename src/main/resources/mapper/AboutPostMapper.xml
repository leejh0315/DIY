<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper	namespace="project.DIY.repository.mybatis.AboutPostMapper">

	<insert id="insertLikes" parameterType="Likes">
		insert into diydb.likepost values(
			#{memberId}, #{postCode}
		);
	</insert>
	
	<select id="selectLikes" parameterType="Likes">
		select count(*) from diydb.likepost where memberid=#{memberId} and postCode=#{postCode};
	</select>
	
	<delete id="deleteLikes" parameterType="Likes">
		delete from diydb.likepost where memberid=#{memberId} and postCode=#{postCode};
	</delete>
	<delete id="deletePostLikes" parameterType="Integer">
		delete from diydb.likepost where postCode=#{postCode};
	</delete>
	
	<insert id="insertNotice" parameterType="Notice">
		insert into diydb.notice values(
			null,
			#{type},
			#{targetId},
			#{targetMemberId},
			#{doMemberId},
			0,
			#{noticeOn}
		);
	</insert>
	
	<select id="selectNoticeById" resultType="Notice">
		select * from diydb.notice where targetMemberId = #{memberId} order by noticeId desc;
	</select>		
	<select id="selectLikedPostsById" parameterType="Integer">
		SELECT postCode FROM diydb.likepost where memberId = #{memberId};
	</select>
	<delete id="deleteNoticeLike" parameterType="Notice">
		delete from diydb.notice 
		where 
		type= #{type} 
		and targetId =#{targetId}
		and targetMemberId = #{targetMemberId}
		and doMemberId = #{doMemberId};
	</delete>
	<update id="updateNoticeView">
		UPDATE diydb.notice
		set view = #{param1}
		where targetMemberId= #{param2};
	</update>
	
</mapper>